services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jupyter
    ports:
      - "8888:8888"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - JUPYTER_TOKEN=password
      # - JUPYTER_PASSWORD=your_password_here  # Uncomment to use password instead of token
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - ./data:/src:rw,cached
    networks:
      - jupyter
    restart: unless-stopped

networks:
  jupyter:
    driver: bridge
